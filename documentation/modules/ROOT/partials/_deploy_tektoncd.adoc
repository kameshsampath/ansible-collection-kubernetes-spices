Tektoncd tasks allows you to configure the Tektoncd installation.

[#tektoncd-ansible-vars]
== Ansible Variables

.Tekton Ansible Variables
|===
| Name | Description | Default Value

| deploy_tektoncd
| To deploy Tekton or not, accepts `true` or `false`
| false

| tektoncd_pipelines_version
| The Tektoncd Pipeline version to install
| v0.25.0

| tektoncd_triggers_version
| The Tektoncd Triggers version to install
| v0.14.2

|===

[#tekton-example-playbook]
== Example Playbook

The following example playbook deploys Tektoncd to the Kubernetes cluster:

[source,yaml,subs="+macros,attributes+"]
----
include::example$tektoncd.yml[]
----
<.> Deploy Tektoncd with defaults

You can check the xref:role-kubernetes-kind.adoc#kind-with-tektoncd[KinD] example to see how to deploy Istio on to a local KinD cluster.