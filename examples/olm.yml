---
- name: "OLM Defaults"
  hosts: spices
  connection: local

  collections:
    - kubernetes.core

  vars:
    deploy_olm: true #<.>

  roles:
    - role: kameshsampath.kubernetes_spices.k8s_app_spices
